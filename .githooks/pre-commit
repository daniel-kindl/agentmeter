#!/bin/sh
set -eu

for path in $(git diff --cached --name-only --diff-filter=ACMR); do
    case "$path" in
        *.jsonl)
            echo "refusing to commit session log data: $path" >&2
            exit 1
            ;;
    esac
done

