# Synthetic test fixtures

Every fixture in this directory is hand-authored to exercise a specific data shape. Fixtures
must not be copied, excerpted, transformed, or derived from real Claude Code or Codex session
logs.

Use invented identifiers, paths, prompts, model names, timestamps, and token counts. A fixture
containing real source code, a real file path, or a real prompt is invalid and must not be
committed.

